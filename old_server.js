/*
    "serve-static": "1.14.1",
	"finalhandler": "1.1.2",
*/

//const static = require("serve-static");
//const finalhandler = require("finalhandler")

/*const staticServer = static("public", {
	maxAge: 1,
	index: "index.txt",
	fallthrough: true,
	//redirect: false
});*/

/*staticServer(request, response, function () {
		StaticServer_Handler(request, response);
	});*/

/*if (error) { // An error as occured
			console.error("> Error serving " + request.url + " - " + error.message);
			response.writeHead(error.status, error.headers);
			response.end();
		} else { // The file was served successfully
			console.log("> " + request.url + " - " + objRes.message);
		}*/

			/*finalhandler(request, response, {
		onerror: FinalHandler_OnError
	})();*/

/*
Events Cycle:
1 - connection
2 - listener
3 - request
*/